mod noise_canceller;
mod recorder;
mod sound_acl;
mod sound_device;
mod sound_player;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
